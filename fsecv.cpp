#include <fstream>
#include <cmath>

using namespace std;

int v[100000];

ifstream f("fsecv.in");
ofstream g("fsecv.out");

int main(){
   int N,Q, st, dr, k, i, j, sq
   f>>N>>Q;
   for(i=1;i<=N;i++){
      f>>v[i];
   }
   sq = sqrt(n);


}
